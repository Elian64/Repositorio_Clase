package Tercer_Trimestre.Trabajos.Ejercicios_Codility

import org.h2.index.Index

/*fun solutionn(H: IntArray): Int {

    var contCuad = 0

        for (i in H.indices) {
            try {
                for (j in H.indices) {
                    if (H[i] < H[i + 1]) {
                        if (H[i] != H[i-1]) contCuad++
                    }
                }
            }catch (e:IndexOutOfBoundsException){"rango no valido"}
        }

    return contCuad
}*/

fun main() {
    var lista: IntArray = intArrayOf(8, 8, 5, 7, 9, 8, 7, 4, 8)

    //println(solutionn(lista))


}